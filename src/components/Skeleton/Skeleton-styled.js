import styled from 'styled-components'

export const RybaStyle = styled.div`
  width: ${(props) => props.$w || '0px'};
  height: ${(props) => props.$h || '0px'};
  background: #313131;
`

export const Test = styled.div``

// 4/5 Указываем ключи
